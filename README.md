# Run Planner

Run Planner is a managed Run Director module for authoring concrete room and
reward routes.

The module is being rebuilt from the locked architecture in
`docs/revamp/README.md`. This branch currently contains only the minimal managed
module skeleton and an unavailable-status tab. It registers no planner editor,
persistence, or runtime routing behavior until the corresponding implementation
checkpoints are complete.
